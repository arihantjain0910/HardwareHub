<nav class="navbar navbar-expand-lg bg-body-light sticky-top">
  <div class="container-fluid">
    <a class="navbar-brand" href="#"
      ><i class="fa-solid fa-computer symbol"></i> HardwareHub</a
    >
    <button
      class="navbar-toggler"
      type="button"
      data-bs-toggle="collapse"
      data-bs-target="#navbarNav"
      aria-controls="navbarNav"
      aria-expanded="false"
      aria-label="Toggle navigation"
    >
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarNav">
      <div class="navbar-nav">
          <a class="nav-link " href="/index">All Assets</a>
          <a class="nav-link" href="/assets/new">Create New Assets</a>
    </div>
    <div class="navbar-nav ms-auto">
      <% if(currUser && currUser.isAdmin === true){ %> 
      <a class="nav-link " href="/signUp">Signup</a>
      <% } %>
      <% if(!currUser){ %>
      <a class="nav-link" href="/login">Login</a>
      <% } %>
      <% if(currUser){ %>
        <a class="nav-link" href="/assets/allCallLogs">All CallLogs</a>
      <a class="nav-link" href="/logout">Logout</a>
      
      <% } %>
    </div>
  </div>
</nav>
